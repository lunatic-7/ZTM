import sys

# It is like modifying our programm from terminal
first = sys.argv[1] # This will simply accept the first word which we type in our terminal.
last = sys.argv[2] # This will simply accept the second word which we type in our terminal.

print(f'hiiii! {first} {last}') # We will run this in terminal.